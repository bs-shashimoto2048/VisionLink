次は todo.md の残りから「backend log の項目をより厳密に揃える」を進めてください。

目的:
Rotate OCR、中央ガイド線基準、Tube判定、OCR前処理、PaddleOCR結果の流れを、ログだけで追えるようにする。
実カメラで読み取り精度や対象判定を調整するため、backend log の粒度と項目名を統一してください。

対象:
- backend/app/services/ai_pipeline.py
- backend/app/services/session_manager.py
- 必要に応じて backend/app/api.py

要件:

1. frame-analyze 1回ごとに、共通の context をログに含めてください。

含める項目:
- session_id
- frame_index
- rotate_left_tube_ocr
- yolo_confidence_threshold
- ocr_confidence_threshold

2. OCR crop ごとのログを統一してください。

各 crop / detection について以下を出してください。

- detection_index
- class_name
- role
- side
- bbox_raw
- bbox_pixel
- image_width
- image_height
- guide_x
- center_x
- is_tube
- is_label
- is_left_of_guide
- rotate_left_tube_ocr
- rotated
- rotation_deg
- crop_shape_before
- crop_shape_after_rotate
- crop_shape_after_preprocess

3. Tube 判定ログを明確にしてください。

ログ例:

```text
OCR target decision detection_index=3 class_name=tube role=tube side=left is_tube=true is_label=false is_left_of_guide=true rotate_left_tube_ocr=true rotated=true
```

また、Tube 判定できない場合は理由を出してください。

例:

* reason=class_name_missing
* reason=class_not_tube
* reason=label_excluded
* reason=right_of_guide
* reason=rotate_disabled

4. OCR前処理ログを統一してください。

preprocess_ocr_crop() で以下を debug ログに出してください。

* original_shape
* final_shape
* ratio
* applied_operations
* skipped_operations
* threshold_value
* clahe_clip_limit
* clahe_tile_grid_size
* resize_mode
* resize_target
* deskew_applied true/false
* deskew_angle if available

5. OCR実行結果ログを統一してください。

各 crop ごとに以下を出してください。

* detection_index
* source
* text
* confidence
* rotated
* rotation_deg
* role
* side
* elapsed_ms

6. frame-analyze 全体の summary ログを追加してください。

最後に info で以下を出してください。

* session_id
* frame_index
* detections_count
* ocr_results_count
* paddleocr_count
* mock_ai_count
* rotated_count
* empty_count
* total_ms

7. mock_ai fallback の理由を明確にしてください。

PaddleOCR から mock_ai に落ちる場合、以下を区別できるようにしてください。

* reason=paddleocr_failed
* reason=paddleocr_empty
* reason=no_ocr_targets
* reason=no_detections
* reason=recognizer_not_available

8. debug crop 保存ログも揃えてください。

OCR_DEBUG_SAVE_CROPS=true のときのみ、保存したファイルパスを debug または info で出してください。

含める項目:

* detection_index
* rotated
* source
* saved_path

9. ログレベルの方針:

info:

* source=paddleocr count
* source=paddleocr empty; fallback=mock_ai
* source=paddleocr failed; fallback=mock_ai
* frame-analyze OCR summary

debug:

* cropごとの詳細
* 前処理詳細
* raw OCR result
* debug crop保存詳細

exception:

* PaddleOCR 実行例外
* 前処理で想定外例外が起きた場合

10. 既存機能は壊さないでください。

維持:

* PaddleOCR → mock_ai fallback
* Rotate OCR: ON/OFF
* 中央赤ガイドライン
* rotated=true の紫色表示
* OCR前処理
* debug crop保存
* frontend 表示

11. 確認してください。

backend:
python -m py_compile backend/app/api.py backend/app/schemas.py backend/app/services/ai_pipeline.py backend/app/services/session_manager.py

frontend:
必要な変更がなければ不要。
変更した場合のみ npm run build。

12. 実行確認で、frame-analyze 1回に対して以下が追えることを確認してください。

* Rotate OCR がONかOFFか
* 各BBoxがTubeかLabelか
* 各BBoxがガイドライン左側か右側か
* 回転対象になったか
* 前処理で何が適用されたか
* PaddleOCRで何が読めたか
* mock_aiに落ちた場合の理由
