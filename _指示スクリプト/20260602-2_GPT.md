次に検出結果の消込処理を追加してください。仕様は以下です。
・ＵＩ上の検査に関わる[カメラ開始][Rotate OCR][検査開始]の各ボタンは検査テーブルをセットしないと押せないようにする。
・チェックは検査テーブルにセットしたテーブルに対し行うこと。
・チェックはＬａｂｅｌの値を基準に行う。
・検出したnmbシリーズの値と同じ検査テーブルのＬａbelの値の行を参照し、その時、検出したnmbの左右にあるTubeの検出結果を、ガイドラインより左なら同じ行のTube_Lの値をチェックし、同じであればテーブルのTube_Lの文字列を検査ＯＫとして緑色にする。右も同様に、ガイドラインより右なら同じ行のTube_Ｒの値をチェックし、同じであればテーブルのTube_Ｒの文字列を検査ＯＫとして緑色にする。
・Tube_L	と Tube_Rが緑文字になった行は左右のチェック完了ということでテーブルのLabel列の文字も緑色に変更し、その行を完了したとして背景をグレーに変更する。
・全ての行のチェックが完了した場合、作業者確認にチェックを入れられるようにし、その後、[完了]ボタンを押下できるようにし、[完了]ボタンを押下後、検査済みということで検査テーブルの3つ目の端子台の選択リスト内の検査済みの端子台の文字列を赤文字へ変更する。

ここまでで確認事項がなければ実装指示を提示して下さい。