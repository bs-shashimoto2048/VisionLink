・検査は消し込んでいく形でチェックしたいので、NGならばそのまま再検査してOKになるまでやり直すようにすること。NGのまま終了させない。
・OKになったものはOKを保持し続ける。破棄するのはNGだった情報のみ。

検査するAPPです。OKした値を保持し続けなければならない仕様は理解できますか？

###


• 実装しました。

  追加した内容

  - frontend/src/types.ts
      - CheckRow に tube_l_status / label_status / tube_r_status / completed を追加
  - frontend/src/App.tsx
      - normalizeCheckText() を追加
      - reconcileCheckRows() を追加
      - checkTable を inspection.detections と ocr_results から再照合して表示
      - selectedSerial / selectedBoard / selectedTerminal 変更時に、検査状態をリセット
      - 完了ボタンを allRowsCompleted と workerConfirmed で制御
      - 作業者確認チェックを全行完了前は無効化
      - CheckDataTable を completed 行の強調表示、OKセルの緑表示に変更
      - console.debug("[VisionLink] check reconcile", ...) を追加
  - frontend/src/styles.css
      - check-cell-ok
      - check-row-completed
      - check-status-mark
        を追加

  確認

  - backend の py_compile 成功
  - frontend の npm run build 成功

  補足

  - todo.md の最新内容に合わせて、今回の中心は「CSVの check table を UI 上で検査結果と突き合わ
    せる」ことに寄せました。
  - 既存の frame-analyze 応答だけで完結するようにしており、バックエンドのレスポンス拡張は入れて
    いません。

  必要なら次に

  1. 照合ロジックを frontend/src/checkReconcile.ts に分離
  2. label / tube_l / tube_r の判定をさらに厳密化
  3. workerConfirmed と完了ボタンの文言を整理

  を続けます。