[33mcommit e2c6d45d54c35ced51877a45c4489e260a6912c6[m[33m ([m[1;36mHEAD[m[33m -> [m[1;32mui/camera-controls[m[33m)[m
Author: hashimoto <shashimoto@betsukawa.co.jp>
Date:   Mon Jun 22 12:05:14 2026 +0900

    feat(ui): カメラ操作UIを整理（設定パネル/表示切替/下部バー）
    
    - 右設定パネルを4調整UI(YOLO閾値/OCR閾値/表示FPS/識別FPS)に作り替え、
      旧情報タイルは復元可能な形でコメント化（機能・stateは維持）
    - 表示切替リストを逆順化し、デフォルトを「推論結果」に変更
    - 映像下部を[表示]/Rotate OCR/カメラ/検査の4操作が常に1列に収まる
      flex(折り返し禁止)バーへ再構成。スマホ幅でのはみ出し防止に
      min-width:0 + overflow:hidden、回転トグルは短縮ラベル(aria/titleは英語フル維持)
    
    Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>

[1mdiff --git a/frontend/src/App.tsx b/frontend/src/App.tsx[m
[1mindex 45cf6d6..9c66850 100644[m
[1m--- a/frontend/src/App.tsx[m
[1m+++ b/frontend/src/App.tsx[m
[36m@@ -20,11 +20,12 @@[m [mimport { CheckStatus, SessionStatus } from "./types";[m
 import type { CheckDataStatus, CheckRow, CheckTableResponse, InspectionSessionResponse, InternalDataLookupResponse, LoginResponse, OCRResult } from "./types";[m
 import { reconcileCheckRows } from "./checkReconcile";[m
 [m
[32m+[m[32m// 表示切替リスト: 順序は現状の逆順、デフォルトは「推論結果」（要件3）[m
 const OVERLAY_MODES = {[m
[31m-  series_conf: "表示: シリーズ+確信度",[m
[31m-  raw: "表示: 取得値そのまま",[m
[31m-  ocr_result: "表示: OCR結果",[m
   inference_result: "表示: 推論結果",[m
[32m+[m[32m  ocr_result: "表示: OCR結果",[m
[32m+[m[32m  raw: "表示: 取得値そのまま",[m
[32m+[m[32m  series_conf: "表示: シリーズ+確信度",[m
 } as const;[m
 [m
 const TEXT = {[m
[36m@@ -62,7 +63,7 @@[m [mfunction App() {[m
   const [banner, setBanner] = useState<string | null>(null);[m
   const [isOnline, setIsOnline] = useState(navigator.onLine);[m
   const [settingsOpen, setSettingsOpen] = useState(false);[m
[31m-  const [overlayMode, setOverlayMode] = useState<keyof typeof OVERLAY_MODES>("series_conf");[m
[32m+[m[32m  const [overlayMode, setOverlayMode] = useState<keyof typeof OVERLAY_MODES>("inference_result");[m
   const [yoloThreshold, setYoloThreshold] = useState(0.25);[m
   const [ocrThreshold, setOcrThreshold] = useState(0.5);[m
   const [rotateLeftTubeOcr, setRotateLeftTubeOcr] = useState(false);[m
[36m@@ -442,47 +443,33 @@[m [mfunction App() {[m
                   <div className="video-badge video-badge-right">検出: {inspection?.detections.length ?? 0}</div>[m
                 </div>[m
 [m
[31m-                <label className="overlay-mode-control">[m
[31m-                  表示切替[m
[31m-                  <select value={overlayMode} onChange={(event) => setOverlayMode(event.target.value as keyof typeof OVERLAY_MODES)}>[m
[32m+[m[32m                <div className="camera-inline-status">[m
[32m+[m[32m                  S:{inspection?.status ?? "待機"} | N:{isOnline ? "ON" : "OFF"} | I:[m
[32m+[m[32m                  {inspection ? `${inspection.performance.yolo_ms}/${inspection.performance.ocr_ms}ms` : "N/A"}[m
[32m+[m[32m                </div>[m
[32m+[m
[32m+[m[32m                {/* 下部操作バー: [表示]セレクタ / Rotate OCR / カメラ / 検査 を常に1列に収める（要件4） */}[m
[32m+[m[32m                <div className="camera-bottom-bar">[m
[32m+[m[32m                  <select[m
[32m+[m[32m                    className="bottom-bar-select"[m
[32m+[m[32m                    aria-label="表示切替"[m
[32m+[m[32m                    value={overlayMode}[m
[32m+[m[32m                    onChange={(event) => setOverlayMode(event.target.value as keyof typeof OVERLAY_MODES)}[m
[32m+[m[32m                  >[m
                     {Object.entries(OVERLAY_MODES).map(([value, label]) => ([m
                       <option key={value} value={value}>[m
                         {label}[m
                       </option>[m
                     ))}[m
                   </select>[m
[31m-                </label>[m
[31m-[m
[31m-                <div className="camera-inline-status">[m
[31m-                  S:{inspection?.status ?? "待機"} | N:{isOnline ? "ON" : "OFF"} | I:[m
[31m-                  {inspection ? `${inspection.performance.yolo_ms}/${inspection.performance.ocr_ms}ms` : "N/A"}[m
[31m-                </div>[m
[31m-[m
[31m-                <div className="camera-control-strip">[m
                   <button[m
                     className={rotateLeftTubeOcr ? "toggle-active" : ""}[m
                     title={rotateLeftTubeOcr ? "左側Tubeの180度回転OCRを無効にする" : "左側Tubeの180度回転OCRを有効にする"}[m
                     aria-label={rotateLeftTubeOcr ? "Rotate OCR: ON" : "Rotate OCR: OFF"}[m
                     onClick={() => setRotateLeftTubeOcr((value) => !value)}[m
                   >[m
[31m-                    {rotateLeftTubeOcr ? "Rotate OCR: ON" : "Rotate OCR: OFF"}[m
[32m+[m[32m                    {rotateLeftTubeOcr ? "OCR回転 ON" : "OCR回転 OFF"}[m
                   </button>[m
[31m-                  <label>[m
[31m-                    YOLO閾値[m
[31m-                    <input type="number" min={0.05} max={0.95} step={0.05} value={yoloThreshold} onChange={(event) => setYoloThreshold(Number(event.target.value))} />[m
[31m-                  </label>[m
[31m-                  <label>[m
[31m-                    OCR閾値[m
[31m-                    <input type="number" min={0.05} max={0.95} step={0.05} value={ocrThreshold} onChange={(event) => setOcrThreshold(Number(event.target.value))} />[m
[31m-                  </label>[m
[31m-                  <label>[m
[31m-                    表示FPS[m
[31m-                    <input type="number" min={1} max={120} step={1} value={displayFps} onChange={(event) => setDisplayFps(Number(event.target.value))} />[m
[31m-                  </label>[m
[31m-                  <label>[m
[31m-                    識別FPS[m
[31m-                    <input type="number" min={1} max={5} step={1} value={analysisFps} onChange={(event) => setAnalysisFps(Number(event.target.value))} />[m
[31m-                  </label>[m
                   <button[m
                     onClick={cameraState.running ? stopCamera : startCamera}[m
                     disabled={!isCheckTableReady && !cameraState.running}[m
[36m@@ -501,7 +488,10 @@[m [mfunction App() {[m
                 </div>[m
               </div>[m
 [m
[31m-              <div className="info-panel">[m
[32m+[m[32m              {/* カメラ右の設定パネル: 旧表示項目(表示FPS/識別FPS の情報タイル)は機能を残したまま表示停止し、[m
[32m+[m[32m                  YOLO閾値 / OCR閾値 / 表示FPS / 識別FPS の4調整UIに作り替え（要件2） */}[m
[32m+[m[32m              <div className="info-panel settings-panel">[m
[32m+[m[32m                {/* 旧表示項目（復元できるよう保持・表示のみ停止）[m
                 <div className="info-tile">[m
                   <span>表示FPS</span>[m
                   <strong>{displayFps}</strong>[m
[36m@@ -510,6 +500,23 @@[m [mfunction App() {[m
                   <span>識別FPS</span>[m
                   <strong>{analysisFps}</strong>[m
                 </div>[m
[32m+[m[32m                */}[m
[32m+[m[32m                <label className="settings-field">[m
[32m+[m[32m                  YOLO閾値[m
[32m+[m[32m                  <input type="number" min={0.05} max={0.95} step={0.05} value={yoloThreshold} onChange={(event) => setYoloThreshold(Number(event.target.value))} />[m
[32m+[m[32m                </label>[m
[32m+[m[32m                <label className="settings-field">[m
[32m+[m[32m                  OCR閾値[m
[32m+[m[32m                  <input type="number" min={0.05} max={0.95} step={0.05} value={ocrThreshold} onChange={(event) => setOcrThreshold(Number(event.target.value))} />[m
[32m+[m[32m                </label>[m
[32m+[m[32m                <label className="settings-field">[m
[32m+[m[32m                  表示FPS[m
[32m+[m[32m                  <input type="number" min={1} max={120} step={1} value={displayFps} onChange={(event) => setDisplayFps(Number(event.target.value))} />[m
[32m+[m[32m                </label>[m
[32m+[m[32m                <label className="settings-field">[m
[32m+[m[32m                  識別FPS[m
[32m+[m[32m                  <input type="number" min={1} max={5} step={1} value={analysisFps} onChange={(event) => setAnalysisFps(Number(event.target.value))} />[m
[32m+[m[32m                </label>[m
               </div>[m
             </div>[m
 [m
