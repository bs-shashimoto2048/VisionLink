Web UI の整理をお願いします。

目的:
CSV由来の検査テーブルを正式な検査テーブルとして使うため、以前の仮データ表示や手修正UIを削除し、カメラ操作まわりをコンパクトに整理してください。

対象:
- frontend/src/App.tsx
- frontend/src/styles.css
- 必要に応じて frontend/src/types.ts

対応内容:

1. 仮データ表示テーブルを削除してください。

削除対象:
以下のカラムを持つ表です。

No | Line | Left | Right | Check

この表は以前の仮検査データ表示なので、画面から完全に削除してください。
関連する state / render 関数 / CSS が未使用になる場合は整理してください。

2. 手修正項目を削除してください。

削除対象:
- 手修正入力欄
- 手修正ボタン
- 手修正対象行選択
- manual-edit API 呼び出しUI
- 手修正に関する画面セクション

注意:
backend API 自体は残して構いません。
今回は Web UI から手修正項目を削除するだけでよいです。

3. CSV由来の検査テーブルは残してください。

残す対象:
- 製番 → 盤番号 → 端子台 の選択UI
- tube_l / label / tube_r を表示する検査テーブル
- 縦スクロール
- sticky header
- ✔ | Tube_L | Label | Tube_R | ✔ | ALL☑ のテーブル

4. カメラ操作・推論設定まわりのレイアウトをコンパクト化してください。

対象項目:
- YOLO閾値
- OCR閾値
- 表示FPS
- 識別FPS
- [カメラ開始]
- [検査開始/停止]

要件:
- Web UI の幅が狭くなっても、なるべく上記項目が1列に収まるようにする
- 入力ボックスとボタンをコンパクトにする
- 横幅に応じて伸縮可能にする
- ラベルと入力欄の余白を詰める
- ボタンは過度に大きくしない
- 可能であれば同じ行に並べ、狭い場合だけ折り返す

5. CSS方針:

以下のようなレイアウトを検討してください。

- 設定行全体:
  display: flex;
  flex-wrap: wrap;
  gap: 6px;
  align-items: end;

- 各設定項目:
  flex: 1 1 90px;
  min-width: 80px;
  max-width: 140px;

- 数値入力:
  width: 100%;
  min-width: 0;
  padding を小さめにする

- ボタン:
  flex: 0 1 auto;
  min-width: 88px;
  padding を小さめにする
  white-space: nowrap;

6. 画面幅が狭い場合の調整:

- 600px 未満でも破綻しないこと
- 入力欄が画面外にはみ出さないこと
- ボタンが極端に大きくならないこと
- 必要に応じて font-size を少し小さくする
- 既存のカメラプレビューや検査テーブルを壊さないこと

7. 表示FPS / 識別FPS の名称は維持してください。

意味:
- 表示FPS: 画面表示更新用
- 識別FPS: frame-analyze 実行頻度用

8. 既存機能を壊さないでください。

維持するもの:
- カメラ開始
- 検査開始/停止
- YOLO閾値
- OCR閾値
- 表示FPS
- 識別FPS
- 表示切替
- OCR結果モード
- YOLO結果モード
- 推論結果モード
- CSV由来の検査テーブル

9. 未使用コードを整理してください。

削除により未使用になった変数、型、関数、CSS クラスがあれば削除してください。
ただし、backend の manual-edit API は今後使う可能性があるため削除しなくて構いません。

10. 確認してください。

- frontend npm run build 成功
- TypeScript エラーなし
- 仮データテーブル No | Line | Left | Right | Check が画面から消えている
- 手修正項目が画面から消えている
- CSV由来の検査テーブルは残っている
- カメラ操作・推論設定の項目がコンパクトに表示される
- 幅を狭めても入力欄とボタンがなるべく1列内で伸縮・折り返しできる