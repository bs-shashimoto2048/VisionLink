# UI_REWORK_TASK.md — カメラ画面のレイアウト整理（スマホ前提・揺れ防止）

## 前提
- 作業ブランチ：`ui/camera-controls`（main 起点、UI 変更が乗っている。e2c6d45 まで）。引き続きここで作業する。**main は触らない**。
- 対象は frontend（React + TS + Vite, **スマホ縦で使う PWA**）。基準はスマホ幅（320〜375px）。
- 前回 e2c6d45 で「表示切替の逆順・デフォルト推論結果」と「下部バーの再構成」は実装済み。
  **ただし4調整値を“映像右の常時表示パネル”に置いたのは誤り**。今回それを設定オーバーレイ側へ作り直す。

## 目標の画面（スマホ縦・1画面・レイアウトは動かさない）
1. タイトル
2. ヘッダー：左「カメラ」／右「設定」ボタン
3. 映像（カメラ）ボックス
4. 操作1行：`[表示] [OCR⤵（Rotate OCR）] [カメラ 開始/停止] [検査 開始/停止]`
5. 「検査テーブル」見出し
6. 列：`[製番] [盤番号] [端子台]`
7. データ行：**この行リストだけが縦スクロール**

「設定」を開くと、4つの調整値（YOLO閾値 / OCR閾値 / 表示FPS / 識別FPS）だけが
**メイン画面にかぶさるオーバーレイ**で出る（レイアウトは押し広げない）。閉じればメインはすっきり。

## ガードレール
- `main` では作業しない。`ui/jcamera-controls` 上のみ。**push / merge はしない**（ユーザーと別途）。
- 「削除」＝**表示から消すだけ。state / ハンドラ / ロジックは残す**（条件レンダリング false 等、戻せる形）。機能を消さない。
- 設定オーバーレイの中身は**4調整値のみ**。
- **検査開始のゲート（テーブル読込まで無効）を壊さない**。
- 変更前に現状を読んで報告（フェーズ0）。迷う所は質問する。
- 確認は**スマホ幅 ＋ キャッシュを回避した状態**で行う（フェーズ8参照）。

## 手順

### フェーズ0：現状把握と報告
読んで報告する。
- 4調整値を今出している“映像右パネル”のコンポーネント
- 「設定」ボタン（設定を閉じる）の開閉の仕組みと、開いたとき現在出ている中身（接続/セッション系）
- 下部バーの現構成
- 検査テーブルの行の描画箇所と、スクロール領域の現状
- **検査開始ボタンが「テーブル読込後のみ有効」になっているか**（なっていなければ「未実装」と明記）
- カメラ初期化箇所と、既定の前面/背面（facingMode）

> 報告後にいったん区切ってから実装へ進む。

### フェーズ1：設定オーバーレイの作り直し（要件2）
- 4調整値（YOLO閾値 / OCR閾値 / 表示FPS / 識別FPS）を**“映像右の常時表示”から外し**、
  「設定」ボタンで開く**オーバーレイの中だけ**に置く。
- オーバーレイは**メインの上にかぶせる**形（position 固定など）。開閉でメインのレイアウトを押し広げない／ガタつかせない。
- 接続/セッション系（再接続・カメラ切替・内部データ参照・ログアウト・QR・注文番号・端末番号・端末名）を
  **UI から非表示**にする。state/ロジックは残す（復元可能な形）。
- 結果：設定を閉じたメイン画面には、調整値もセッション系も出ない。

### フェーズ2：表示切替（要件3）※実装済みの確認のみ
- 逆順（推論結果 → OCR結果 → 取得値そのまま → シリーズ+確信度）／デフォルト「推論結果」が保たれているか確認する。

### フェーズ3：下部操作1行（要件4）
- 下部を `[表示]/[OCR⤵]/[カメラ 開始-停止]/[検査 開始-停止]` の**4操作・常に1列・折り返し禁止**にする。
- OCR⤵ は操作1行のまま（設定へは移さない）。
- スマホ幅で折り返し・はみ出しが出ないこと（必要に応じ幅/余白/ラベルを詰める）。

### フェーズ4：検査テーブルの行スクロール
- データ行を**固定高さのスクロール領域**（`overflow-y: auto`）に入れ、全行はスクロールで到達できるようにする。
- テーブル見出し（製番/盤番号/端子台）とその上（映像・操作1行）は固定。**行が増えてもページが伸びて飛ばない**こと。

### フェーズ5：画面揺れ防止
- 映像ボックスは**固定アスペクト/固定高さ**で確保し、カメラ起動でサイズが変わって飛ばないようにする。
- 検出/OCR の状態表示（稼働・検出数・レイテンシ等）は、フローを動かさないよう**映像の上に固定オーバーレイ**で出す。
- ある行が消込まれたら、スクロール領域内で**該当行へ自動スクロール／ハイライト**する。
- 毎フレームの更新で、フロー内要素の高さが変わらないようにする（文字数の変化で揺れない／領域を確保）。
- モバイルのアドレスバー伸縮で揺れないよう、ビューポート基準を安定させる（例：`dvh` や固定レイアウト）。

### フェーズ6：検査開始ゲートの保持
- 「検査開始はテーブル読込後のみ有効」を、今回の変更後も維持する
  （フェーズ0 で未実装と判明した場合は、その旨を報告。実装の要否は別途相談）。
- テーブル読込は **製番/盤番号/端子台** のドロップダウンだけで成立する前提（セッション系を隠してもこの経路は塞がない）。

### フェーズ7：カメラ既定（実装注意）
- カメラ切替を非表示にするので、**起動時の既定を背面（`facingMode: "environment"`）**にする
  （前面のままだと端子台に向けられない）。

### フェーズ8：確認
- `npm run build` 通過。
- dev サーバ起動 →**スマホ幅（iPhone SE / 320〜375px）**で目視。
  - **Service Worker キャッシュに注意**：シークレットウィンドウで開くか、DevTools → Application → Service Workers を
    Unregister ＋ Clear storage してから確認する（前回これで「反映されない」誤認があったため）。
- チェック項目：
  - 設定を閉じたメインに、調整値もセッション系も出ていない
  - 「設定」を開くと4調整値だけがオーバーレイで出て、開閉でレイアウトが動かない
  - 下部が `[表示]/[OCR⤵]/[カメラ]/[検査]` の1列
  - データ行がスクロールでき、操作中に周囲のレイアウトが揺れない
  - 検査開始がテーブル読込前は無効、読込後に有効
  - カメラ既定が背面
- 結果を報告する。

### 仕上げ
- `ui/camera-controls` に意味のあるメッセージで1コミット。
- 変更点と確認結果を報告。push / merge はユーザーと相談（このタスクでは行わない）。